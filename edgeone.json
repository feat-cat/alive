{
  "installCommand": "npm install",
  "buildCommand": "npm run build:makers",
  "nodeVersion": "24",
  "agents": {
    "framework": "openai-sdk",
    "timeout": 120,
    "sandbox": {
      "timeout": 300
    }
  },
  "schedules": [
    { "name": "heartbeat", "cron": "0 3 * * *", "path": "/heartbeat", "method": "POST" }
  ]
}
